\begin{pycode}
qno = <<<qno>>>
group = <<<group>>>
\end{pycode}
\textbf{Integration Problems.} Compute the following integrals:
\begin{enumerate}
\item[a.] What is the indefinite integral of \(f(x)=3x^2+2x+1\)?\ifshowsolutions\ \ \ \textcolor{blue}{\(\int f(x)dx = x^3 + x^2 + x + C\)}\fi
\ifshowsolutions
\else
\vspace{1.4cm}
\fi
\begin{pycode}
if 'AnsSet' in locals():
    AnsSet.register(qno, group=group, label='(a)', value=r'\(x^3 + x^2 + x + C\)')
\end{pycode}
\item[b.] What is the definite integral of \(f(x)=\cos(x)\) from \(0\) to \(\pi/2\)?\ifshowsolutions\ \ \ \textcolor{blue}{\(\int_0^{\pi/2} \cos(x) dx = 1\)}\fi
\ifshowsolutions
\else
\vspace{1.4cm}
\fi
\begin{pycode}
if 'AnsSet' in locals():
    AnsSet.register(qno, group=group, label='(b)', value=r'\(1\)')
\end{pycode}
\item[c.] What is the indefinite integral of \(f(x)=e^{3x}\)?\ifshowsolutions\ \ \ \textcolor{blue}{\(\int f(x)dx = \frac{1}{3} e^{3x} + C\)}\fi
\ifshowsolutions
\else
\vspace{1.4cm}
\fi
\begin{pycode}
if 'AnsSet' in locals():
    AnsSet.register(qno, group=group, label='(c)', value=r'\(\frac{1}{3} e^{3x} + C\)')
\end{pycode}
\end{enumerate}

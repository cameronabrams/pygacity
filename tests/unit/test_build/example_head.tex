\pagestyle{fancy}
\fancyfoot[CE,CO]{p. \thepage\hfill\tiny{<%serial%>}}

\noindent This is an example header named \textcolor{blue}{\tt \currfilename}.

\begin{pycode}
from sandlersteam.state import State as SANDLER
from sandlersteam.state import SteamTables as st
import pygacity.texutils as tu
import numpy as np
import fractions as fr
import yaml
from argparse import Namespace
from math import pow
from pygacity.pick import Picker
P=Picker(<%serial%>)
suphPavail=st['suph'].uniqs['P']
solutions=[]
\end{pycode}
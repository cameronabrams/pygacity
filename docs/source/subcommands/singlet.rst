.. _singlet:

Building a Single-Problem Document
----------------------------------

``pygacity singlet`` allows you to build a document with a single problem to test your typesetting.  If your tex source is ``file.tex``, just do this:

.. code-block:: bash

    $ pygacity singlet -t file.tex

By default, the assembled results will appear in the ``build-singlet`` directory.
